# Session Handoff (2026-04-04)

## Context
- Repo: /Users/kouiso/ghq/kouiso/adult-ai-app
- Branch: main, Latest: eb8cce8
- Uncommitted: 11 files
- Dev: pnpm dev:worker (port 8788)
- Seed: pnpm db:seed:reset (restart wrangler after)

## Done
- Prompt rewrite (src/lib/prompt-builder.ts: WHO YOU ARE + REALISM)
- Scene phase split (functions/api/[[route]].ts: PLATFORM_BASE / SCENE_RESPONSE_STRUCTURE)
- Novita API fix (meinahentai_v4, request wrapper, passthrough schema)
- Image prompt LLM translation (JP->EN + characterDescription)
- Character list bug fix (userId field added to GET /characters)
- Seed USER_ID changed to local-dev@adult-ai-app.local
- Full scenario test: pickup->hotel->creampie (loop issue found)
- Anime image generation working (meinahentai_v4)

## Remaining (3 tasks)

### 1. Loop detection fix (CRITICAL)
Symptom: creampie scene loops "もう死にそう おかしくなりそう" infinitely
Cause: src/lib/api.ts:66 split regex misses space-separated repetition
Fix: Change split(/[\n…。！？]+/) to split(/[\n…。！？\s]+/)
Also consider: REPETITION_CHECK_WINDOW 500->300

### 2. Character name shows "AI" on new conversation
Symptom: After selecting character, new conversation shows "AI" not character name
Investigate: chat-view.tsx:138,232 / route.ts:359 / character-manager.tsx:452
Root: activeCharacterId not passed or DEFAULT_CHARACTER_ID used

### 3. Commit uncommitted changes (11 files)

## Setup
pnpm db:seed:reset && restart wrangler && pnpm dev:worker
Clear SW cache before Playwright tests

## Key Files
- src/lib/prompt-builder.ts (BASE_RULES)
- src/lib/api.ts (detectRepetition, generateImage)
- src/component/chat/chat-view.tsx (buildApiMessages, handleGenerateImage)
- functions/api/[[route]].ts (Hono API, Novita)
- scripts/seed.ts (40 chars + 40 convos)
- src/store/settings-store.ts (activeCharacterId)
