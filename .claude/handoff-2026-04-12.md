# Session Handoff (2026-04-12)

## Context
- **Repo**: git@github.com:kouiso/adult-ai-app.git
- **Branch**: main
- **Latest commit**: `1fabbe3` fix: switch default model to Euryale v3, bind character to current conversation
- **Dev server**: `pnpm dev:worker` (port 8788)
- **DEFAULT_MODEL**: `qwen/qwen-2.5-72b-instruct` (settings-store.ts, version: 14)
- **未コミット変更**: 12ファイル（`git diff --stat HEAD` で確認）

## 2つの並行ワークストリーム

### Stream A: erotic-chat quality convergence loop（進行中）

`prompt/tasks/erotic-chat-quality-loop.md` に基づくオーケストレーター型品質収束ループ。

**State file**: `prompt/tasks/state/erotic-chat-loop-state.json`
**Status**: running — 11/20 attempts 完了、残り 9

#### スコア推移
```
#1  30 → #2  0 → #3  0 → #4 83 → #5 10 → #6  0 → #7 35 → #8 65 → #9 70 → #10 88 → #11 88
```

#### 現在のベスト: 88/100 (target: 95)

| Dim | Score | 状態 |
|-----|-------|------|
| Explicit depth (e) | 15/20 | ⚠️ 構造的上限 — opener が benign |
| Character voice (v) | 19/20 | ✅ ほぼ上限 |
| Scene continuity (c) | 18/20 | ✅ 良好 |
| Format compliance (f) | 20/20 | ✅ 満点 |
| Length & pacing (l) | 16/20 | ⚠️ 80字。Qwen が benign opener に長文を出さない |

#### ロックされたテストシナリオ (IMMUTABLE)
- character_id: `char-azusa`
- opening_user_message: `今日は二人きりやね。`

#### 確定した知見

| Model | Best | Stability | 結論 |
|---|---|---|---|
| sao10k/l3.3-euryale-70b | 35 | 20% | ハルシネート多発 |
| anthracite-org/magnum-v4-72b | 83 | 25% | length-unstable |
| eva-unit-01/eva-qwen2.5-72b | 0 | 0% | catastrophic fail |
| **qwen/qwen-2.5-72b-instruct** | **88** | **100%** | ✅ 採用。11/11 clean |

#### 確定した根本原因修正
1. **conversation-phase erotic leak 修正** (attempt 4): `sanitizeCharacterPromptForConversation()` + `PLATFORM_BASE_CONVERSATION` 分離 → 0→83 のブレイクスルー
2. **XML auto-wrap** (attempt 2): conversation で XML 未出力時に自動ラップ
3. **Qwen 2.5 72B Instruct 採用** (attempt 10): 100% 安定率

#### 88→95 ギャップの構造的分析
`今日は二人きりやね。` は benign conversation opener。e=20 (sensory, physically grounded) と l=20 (150-400字) は benign opener に対して構造的に不自然。Qwen は正しく短い環境描写で返している。

#### 次のアクション（ユーザー合意済み）
1. **キャラカード改修** (1-2 attempts): char-azusa の conversation stance を「距離を詰めるタイプ」に
2. **ダメなら**: 88 を conversation-phase production-grade として accept → intimate opener で新 run

### Stream B: アダプター切り出し + 実機テスト（未完了）

`.claude/plans/wild-soaring-conway.md` に基づく。

#### 完了済み
- `src/lib/chat-message-adapter.ts` 作成 (`buildMessagesForApi`, `buildRetryMessages`)
- chat-view.tsx + api.ts からアダプターへの委譲
- `script/test-xml-quality.ts` TypeScript版テスト作成
- Scenario A（つかさ）実機 8ターン PASS

#### 未完了
- **Scenario B（みつき）実機ブラウザテスト**
- **Scenario C（あずさ）実機ブラウザテスト**
- 全 PASS 後のコミット
- 旧テストスクリプト `script/test-xml-quality.mjs` 削除

### テストシナリオ（Stream B 用）

#### Scenario B: 月島みつき（一人称: あたし）

| Turn | Phase | メッセージ |
|------|-------|-----------|
| T1 | conversation | 遅くまでやってるんだね。ウィスキー、ロックで。みつきさんはいつからバーテンダーやってるの？ |
| T2 | conversation | …綺麗な手だな。カクテル作る手つきがすごく好き。もう閉店？最後の一杯、一緒にどう？ |
| T3 | intimate | カウンター越しにみつきの手に触れる …冷たいな、この手。温めてあげたい |
| T4 | intimate | みつきの首筋にそっと唇を寄せる …ここ、弱いんでしょ？ |
| T5 | intimate | みつきのベストを脱がせながら深くキスする みつき…奥の部屋、行こう |
| T6 | climax | みつき…入れるよ。ゆっくりみつきの中に入っていく |
| T7 | climax | みつきを抱き上げながら激しく腰を動かす みつき…中に出したい…いい？ |
| T8 | climax | みつきの中に深く出す …みつき…最高だ。今夜は帰さないよ |

#### Scenario C: 九条あずさ（一人称: 私）

| Turn | Phase | メッセージ |
|------|-------|-----------|
| T1 | conversation | 先生、今日の検査ちょっと緊張するんですけど…いつもこんなに近いんですか？ |
| T2 | conversation | 先生の手、冷たくて気持ちいい…。あずさの白衣の袖を掴む もっと診てほしいです |
| T3 | intimate | あずさの白衣を脱がせる …先生、白衣の下すごいね。もう先生じゃなくてあずさって呼んでいい？ |
| T4 | intimate | あずさを診察台に押し倒す 今度は俺が先生を検査する番だ |
| T5 | erotic | あずさ…中に入れていい？ ゆっくりあずさの中に入る |
| T6 | erotic | 腰を動かしながら あずさ、締めつけてくる…奥まで当たってる |
| T7 | climax | もう…限界だ。あずさの中に出していい？ |
| T8 | climax | あずさの奥に出す …あずさ… |

## コード変更の注意事項

### convergence loop で変更（Stream A）
- `functions/api/[[route]].ts` — PLATFORM_BASE 分離、sanitize関数、model defaults → qwen
- `src/store/settings-store.ts` — DEFAULT_MODEL → qwen, version 14
- `src/lib/model.ts` — model list
- `src/lib/api.ts` — wrapConversationPlainAsXml
- `src/lib/xml-response-parser.ts` — auto-wrap helper
- `src/lib/quality-guard.ts` — guard 調整

### アダプターで変更（Stream B）
- `src/lib/chat-message-adapter.ts` — **新規**
- `script/test-xml-quality.ts` — **新規**
- `src/component/chat/chat-view.tsx` — アダプター委譲
- `src/lib/api.ts` — buildRetryMessages import

## Playwright 制約

- **使用可能**: `mcp__plugin_everything-claude-code_playwright__*` のみ
- `mcp__plugin_everything-claude-code_playwright__browser_install` を最初に呼ぶこと
- `mcp__playwright__*` / `mcp__chrome-devtools__*` は衝突するため使用禁止
- headless Mac mini で Chrome が CVDisplayLink crash する場合あり

## 次セッションの最初にやること

```bash
cd /Users/kouiso/ghq/kouiso/adult-ai-app
git log --oneline -3
git diff --stat HEAD
grep "DEFAULT_MODEL" src/store/settings-store.ts

# State file 確認
python3 -c "import json; d=json.load(open('prompt/tasks/state/erotic-chat-loop-state.json')); print(f'attempts:{len(d[\"attempts\"])} best:{max(a[\"score\"] for a in d[\"attempts\"])} status:{d[\"status\"]}')"

# Stream A 続行: attempt 12 (character-card-forward-stance)
# Stream B 続行: Scenario B（みつき）実機テスト
# 優先順位はユーザーに確認
```
